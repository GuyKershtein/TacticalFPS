// miniaudio is a single-header library: exactly one translation unit must
// define MA_IMPLEMENTATION before including it, to actually emit its
// function bodies. This file exists solely for that purpose — nothing else
// should include miniaudio.h with MA_IMPLEMENTATION defined.
#define MA_IMPLEMENTATION
#include "miniaudio.h"
